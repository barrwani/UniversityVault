views:
  - type: table
    name: Table
    filters:
      and:
        - file.folder.startsWith("University/CS")
    order:
      - file.name
    sort:
      - property: file.name
        direction: ASC
